# Brand constants — one of exactly two files that may contain the project name.
# The other is pyproject.toml [project].name.
# All other source files must use generic names (e.g. "agent").
__brand__ = "kayaclaw"
__slug__ = "kayaclaw"
__version__ = "0.1.6"

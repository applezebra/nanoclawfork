"""Brand-name constants.

RENAME SITE 2 OF 2 — see BRANDING.md.

Only these constants reference the brand name. Everything else in the
codebase uses the generic `agent` module path and `AGENT_*` env vars.
"""

__brand__ = "NanoClaw Fork"   # human-facing display name
__slug__ = "nanoclawfork"      # lowercase, no-space identifier
__version__ = "0.0.1"

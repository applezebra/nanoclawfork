# 0.1 Contract — Locked

This file is the **testable definition of done** for v0.1. It is the input to
spec-writer (formal product spec) and impact-analyzer (delivery plan).

Any item not listed here is out of 0.1 scope. Bring scope changes back to a
contract revision, not into the build.

---

## Project identity

- **Brand name:** `kayaclaw` (locked 2026-05-03 — Singapore-rooted, impartial; .ai and .com both registered. Original working name `nanoclawfork` retained in some historical references and the local working dir for context continuity.)
- **Version target:** `0.1.0`
- **License:** MIT
- **Public posture:** **private repo during build; flip to public ONLY when 0.1 is feature-complete and security-audited.** (Updated 2026-05-03 by Anson — original contract said "public from first commit"; corrected to avoid shipping mid-build code with unaudited Telegram input surface.)
- **Positioning:** "An LLM-agnostic NanoClaw fork — same security posture, no Anthropic lock-in."

## Thesis (must hold for any 0.1 build)

NanoClaw's container-isolated security model + an LLM-agnostic core. **Provider-impartial:**
no specific LLM provider (Anthropic, OpenAI, DeepInfra, Ollama, etc.) is a hard dependency
or a hard-coded default. Users choose their provider via `config.yaml`. Anthropic is welcome
as a configured provider — it just isn't required, and isn't the default.

## Works in 0.1 (the contract — every item must be true)

1. **Telegram connector**, text-only, single chat-id allowlist (`ALLOWED_TELEGRAM_CHAT_IDS` env var). [Renamed from `ALLOWED_TELEGRAM_USER_IDS` per L4 plan-eng-review A1: `chat_id == user_id` in DMs but diverges in groups; the var is named by what we actually check.]
2. **DeepInfra provider** end-to-end via OpenAI-compatible API. Default model: Llama 3.3 70B Instruct.
3. **PydanticAI runtime**, with no provider-vendor SDK (Anthropic, OpenAI, etc.) as a hard *top-level* dependency. The deployed install pulls in the OpenAI SDK transitively via `pydantic-ai-slim[openai]` (the slim variant; the full `pydantic-ai` meta-package was rejected during L5 Step 2 because it pulls in `anthropic` SDK by default, violating AC-4). The Anthropic SDK is verifiably absent from the runtime container. The default `config.example.yaml` ships pointing at a non-Anthropic provider (DeepInfra) so that the out-of-box install does not require an Anthropic account or API key.
4. **SQLite memory** on a named Docker volume, per-chat history, survives container restart.
5. **Hardened container**: 16 of 20 controls in `SECURITY.md` verified (the 4 deferred are explicitly tracked).
6. **Local Docker** on Anson's Linux machine. `docker compose up` is the deploy command.
7. **Provider registry** loaded from `config.yaml`. DeepInfra configured. Schema supports adding more providers without code changes.
8. **Brand-decoupled code**: project name lives in exactly two files (`pyproject.toml`, `agent/__about__.py`).
9. **Public repo at 0.1 release** (NOT during build — see Public Posture above), MIT license, `NOTICES.md` attributing any lifted MIT-licensed code. Flip-to-public is the final action of the L6 release polish lane, gated on security-auditor pass.
10. **README** documents what works, what's in progress, what's explicitly out of 0.x.

## Stretch (only if 0.1 lands fast — do not block 0.1 release on these)

- **Ollama** as a second provider in the registry, proves the "registry isn't a one-off" claim.

## Explicitly NOT in 0.x (locked — bring to 1.x discussion only)

- Web UI / dashboard
- Multi-user, multi-tenant, or multi-bot deployment
- WhatsApp, Slack, Discord, Gmail connectors
- Tool / function calling beyond a text reply (no file ops, web search, bash-in-container)
- Scheduled jobs / cron
- Cost-based provider routing, fallback chains, or per-message routing
- VPS deployment automation
- Streaming responses
- Vector recall / RAG
- Episodic memory summarization
- Image, audio, video input

## Done = all of the following

A reviewer can verify each independently:

- ✅ `docker compose up` starts the agent
- ✅ Sending a Telegram message from an allowlisted chat ID returns a reply
- ✅ Reply was generated by DeepInfra Llama 3.3 70B (verifiable via DeepInfra dashboard or response metadata)
- ✅ Default install + default `config.example.yaml` does not contact `api.anthropic.com` (proves no Anthropic dependency by default; users who configure Anthropic as their provider WILL contact it — that's the point of being agnostic)
- ✅ Conversation persists across `docker compose down && docker compose up`
- ✅ Container runs as non-root, read-only root FS, dropped capabilities (verifiable via `docker inspect`)
- ✅ All 16 of 20 controls in `container/SECURITY.md` are checked off with verification notes
- ✅ README's "What works" section contains the same statements as this contract

## Build approach

- **Derivative work**, not literal git fork. Fresh repo, fresh runtime.
- **Lift candidates from `qwibitai/nanoclaw`** (MIT, attribution in `NOTICES.md`):
  - Telegram connector — pending security-auditor approval
  - Container Dockerfile patterns — reference, not lift
  - Mount allowlist concept — reference, not lift
- **Internal package name:** `agent/` (generic, survives rename).
- **Env var convention:** `AGENT_*` (e.g. `AGENT_DATA_DIR`).

## Resolved decisions (auto-resolved from spec-writer's open questions)

| Decision | Choice | Rationale |
|---|---|---|
| Config hot-reload | Not in 0.1 — restart required | Simpler, less code, fits the "small and auditable" thesis |
| Unauthorized message rejection | Silent drop | Standard practice for personal bots; lower discoverability for attackers |
| Context-window overflow | Keep last 20 turns, drop older | Sliding window is 0.2+ work |
| Misconfig at startup | Crash loudly with clear error | Fail-fast posture for a security-focused tool |

## Anti-bloat discipline

Anti-bloat is a permanent SDLC guardrail, not a one-time check. See
`docs/REVIEW-PROTOCOL.md` for the full anti-bloat gate that runs at every
review point. Core principles:

1. Small beats clever.
2. No speculative abstraction (no interfaces/plugins until 2nd concrete use).
3. No premature DRY (3 similar lines beat a wrong abstraction).
4. No silent feature creep (one commit, one purpose).
5. Document WHY, not WHAT.

Every reviewer at every gate runs the 6-question anti-bloat checklist in
REVIEW-PROTOCOL.md before approving.

## Reference material (in this repo, not in build path)

- `docs/discovery/` — pre-SDLC scaffold. Read for design intent. Do not lift code without re-derivation through the SDLC.
- `docs/discovery/container/SECURITY.md` — 20-control checklist (this is the source of truth for security posture).
- `docs/discovery/config.example.yaml` — provider registry schema.
- `docs/discovery/agent/providers.py` — provider registry pattern sketch.

---

## Size Discipline (locked)

Auditability is a core differentiator. NanoClaw markets itself as "small," but
the actual upstream (qwibitai/nanoclaw, main branch, audited May 2026) is
**~7,645 effective LOC** of TS source (blank/comment-stripped, excluding tests).
The Telegram connector + chat-sdk bridge alone is ~1,000 effective LOC.

Our 0.1 is committed to fitting in **less than 1/10 of NanoClaw's current size**.

### Per-module LOC budgets (effective LOC, blank/comment stripped)

| Module | Target | Hard cap |
|---|---|---|
| Telegram connector | 150 | 250 |
| Provider registry | 80 | 150 |
| Runtime wrapper (PydanticAI) | 50 | 100 |
| Memory (SQLite) | 80 | 150 |
| CLI entrypoint | 30 | 50 |
| Config schema (Pydantic models) | 60 | 100 |
| **Total core Python** | **450** | **800** |
| Dockerfile + entrypoint.sh | 40 | 60 |
| docker-compose.yaml | 40 | 60 |

### Enforcement points

1. **plan-writer** must hand each lane a LOC budget. If a lane plan implies a module exceeding its hard cap, the lane is split or scope-cut before code-implementer begins.
2. **code-implementer Tiny Steps** (≤300 LOC, ≤3 files per step) — already in CLAUDE.md.
3. **`/simplify` skill** runs after each landed step on the diff.
4. **`code-reviewer` Gate 0 (structural health)** runs at the 500 LOC project milestone — full structural pass for files >400 LOC, functions >50 LOC, abstraction opportunities.
5. **`/codex-review`** on every staged diff — already in CLAUDE.md.
6. **Hard rule:** if a module hits its hard cap before its lane is complete, **stop and re-plan** — do not push through. Re-planning options: (a) scope-cut, (b) extract a follow-up to 0.2, (c) accept the cap breach and document why.

### Acceptance criterion (added to "Done =" list)

- ✅ Total core Python ≤ 800 effective LOC at release. Per-module counts ≤ each module's hard cap.
- ✅ README's "Status" section states the actual measured LOC count (e.g. "0.1 ships at 487 effective LOC of core Python").
- ✅ A `scripts/loc.sh` (or equivalent) exists and is reproducible by any reader.

### Honest positioning claims (use in README, not before)

Claims we can make once 0.1 lands within budget:

- *"~10× smaller than NanoClaw's current source size."* (vs. 7,645 effective LOC)
- *"Our entire 0.1 fits inside what NanoClaw spends on Telegram alone."* (vs. ~1,000 LOC for connector + bridge)
- *"Auditable in an afternoon."* — true if we hit the 450 target.

Claims we will NOT make:

- "Smaller than NanoClaw 0.1" — not audited, probably not true at fresh-start parity.
- "Most secure self-hosted agent" — unverifiable, marketing-shaped.
